import { TransportConnectOptions } from '../interfaces';
import { Stan } from 'node-nats-streaming';
export declare const createConnection: (clusterID: string, clientID: string, connectOptions: TransportConnectOptions) => Promise<Stan>;
